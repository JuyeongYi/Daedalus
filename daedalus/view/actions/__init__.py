# daedalus/view/actions/
"""편집 액션 — **UI 표면과 무관한 기능 실체** (A8/A9).

이 패키지의 함수가 기능이고, 캔버스 우클릭 메뉴와 에디터 위젯은 둘 다 **호출부**일
뿐이다. 한쪽에 로직을 넣고 다른 쪽이 흉내 내면 "같은 조작인데 어디서 했느냐에
따라 결과가 다른" 상태가 되고, 그건 협업 도구로 실격이다(컴파일 LOCAL 설치와
"Claude Code 실행" 메뉴가 `wiring.wire_workspace`를 공유하는 것과 같은 결).

규약:
- 입력은 모델/뷰모델(component·project·project_vm)이고 위젯을 받지 않는다.
  다이얼로그가 필요한 액션은 **호출부가 값을 물어 온 뒤** 그 값을 넘긴다.
- 편집은 `ProjectViewModel.execute`(CommandStack)를 거친다 — 사용자가 Ctrl+Z로
  되돌릴 수 있어야 한다.
- 테스트는 여기 함수 단위로 쓴다. 호출부는 "이 함수를 부르는가"만 확인하면
  되고, 로직 테스트를 표면마다 복제하지 않는다.
"""
