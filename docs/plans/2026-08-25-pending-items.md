# 남은 항목 (2026-08-25 세션 마감 시점)

> A1~A12 백로그·WP-RF·WP-BB(CLI C+A)·WP-SF·WP-BO가 전부 master에 랜딩된
> 시점의 잔여 목록. 전체 테스트 2,018 passed.

## 1. 결정 대기 — A5 컴파일 분할 (점진 공개)

설계 문서: `docs/plans/2026-08-25-a5-progressive-disclosure.md` (구현 금지 상태).
착수하려면 아래 7건 확정 필요 — 표는 설계자 권고.

| # | 결정 | 권고 |
|---|---|---|
| D1 | 분할 기본값 | 프로젝트 속성 토글, 기본 off (기존 산출 불변) |
| D2 | SPLIT_THRESHOLD | 실측 후 결정 (근거 없는 숫자 금지) |
| D3 | SECTION_MIN | 실측 후 결정 |
| D4 | 색인 "언제 읽어라" 문구 출처 | 섹션 첫 문장 발췌 + 고정 문구 폴백 |
| D5 | 한글 제목 슬러그 | `section-N` (인코딩 회피, 결정적) |
| D6 | 분할 결과 미리보기 | 조각까지 함께 (임계값 조정 근거) |
| D7 | 분할 시 경고 | 정보성 1건 |

## 2. 개선 후보 (미확정 — 필요해질 때)

- **컴파일 미리보기 비모달화** — 현재 모달 다이얼로그(A9-1)라 편집하며 나란히
  못 본다. 도킹 패널화 후보.
- **참조 하이라이트의 선택 상태 차용** — 2초 뒤 `clearSelection()`이 사용자
  선택까지 지운다(A9-6 트레이드오프). 별도 이펙트 아이템으로 교체 후보.
- **호출 계약 줄 길이** — 포트 설명+transfer 설명+전제 지시가 한 줄. 항목 많은
  에이전트에서 길다.
- **카탈로그 편집 UI (구 A4)** — 현재 폴더 열기만. 목록+폼 패널/다이얼로그/README
  시딩 3안 제시 상태로 미확정.
- **훅 전역 스코프 편집 UI** — A1은 읽기 전용 표시 + 폴더 열기까지. 전역 훅
  직접 편집 UI는 범위 밖으로 남김.
- **파일 크기 분해 후보** — app.py(1,145) / skill_editor.py(1,101) 다음 순번.
  scene.py는 context_menus 분해로 1,082. (상한 1,200은 테스트가 강제 중.)
- **A11 테스트 docstring 한영 혼용** — 일괄 치환 부수 효과. 사소.

## 3. 도그푸딩 재개 절차

1. 앱 재시작 (오늘 랜딩 전부 반영: A1~A12, transfer 배선, 영어 산출,
   tri-state, 진입점 프리셋, 라이프사이클 피커, 미저장 확인).
2. `-old` 스킬 2종은 delete_component(MCP)로도 삭제 가능해졌음(A2) — 정리.
3. test 폴더 재컴파일 — 산출 전면 갱신(영어화 + transfer 문구 + tri-state
   명시 발행) 확인.
4. transfer 실사용 검증: 전이에 부착 → 출발 Next Steps / 도착 계약 확인.

## 4. 참고

- `hooks-lifecycle-dark.svg`(저장소 루트)는 A10 라이프사이클 피커 `_LAYOUT`의
  출처 문서로 함께 커밋됨 — 좌표·색 변경 시 이 파일 기준으로 대조.
- 이벤트 **집합** 드리프트는 테스트가 잡지만(_LAYOUT ↔ HookEvent 일치) 좌표·색
  변화는 수동 대조.

## 5. 보류 (2026-09-06 추가)

- **WP-WS 작업 폴더 설정 편집** (사용자 보류 확정): settings.local.json의
  permissions(allow/deny/ask)·env 등을 설계 모델에서 편집·배출하는 기능.
  동기는 실재한다 — 훅 차단은 스크립트가 돌아야 막지만 `permissions.deny`는
  CC 권한 시스템의 선언적 강제라 우회가 없다(state/** deny + 블랙보드 연동
  자동 합성 아이디어 포함). **다만 설정 가능한 항목이 매우 많아 표면 전체를
  제대로 기획한 뒤 착수한다** — 부분 구현으로 시작하지 않는다. 착수 시
  별도 계획 문서부터.
- **B2 기존 플러그인 임포트** — 개시 미정 (docs/plans/2026-09-06-improvement-batch.md 참조).

## 6. 배치 완료 직후 작업 큐 (2026-09-06) — 전부 완료

1. ~~새 프로젝트 통합 다이얼로그~~ (877c502) 2. ~~트랜스퍼 미리보기~~ (70d567c)
3. ~~scriptName .sh.sh~~ (06761f7). 폴더형 템플릿도 완료(f35b7f6).

## 8. MCP 패리티 배치 (2026-09-07 사용자 확정)

**원칙(상시): GUI에서 가능한 모든 동작은 MCP로도 가능해야 한다** — CLAUDE.md
WP-MCP 섹션에 기록. 새 GUI 기능은 대응 MCP 도구를 같은 WP에서 함께 만든다.

이번 배치 범위 — A3 실측 보고 전체 소거 + 리뷰 소품:
- 편집 갭 G1~G16 전체 (P1: G1 블랙보드 클래스 수정·삭제·개명 / G2 필드 단위
  편집 / G3 컴파일 dry-run — 컴파일러 emit 경고 가시화. P2: G4 진행 훅 토글 /
  G5 진입점 프리셋(apply_entry_preset 직호출, 1 undo) / G6 call_agents 수정
  (set_transfer_on 짝) / G7 전역 훅 조회+프로젝트로 복사 / G8 훅 프리셋 생성 /
  G9 카탈로그 후보 조회 / G11 new_project(template_id, build_target — 통합
  다이얼로그와 동형, 저장 게이트). P3: G10 웨이포인트 / G12 새 프로젝트·패키지
  가져오기 / G13 참조 이동 / G14 생성+배치 1 undo(creation.make_component
  환원 — S1 팩토리 3중복 해소 동반) / G15 transfer 생성+할당 1 undo /
  G16 focus_node·select_nodes)
- 조회 낭비 Q1~Q6 (P1: Q1 get_project 훅 스크립트 전문 축약 + get_hook 신설 /
  Q2 전이 guard·waypoint_count 노출. P2: Q3 config 비기본값만 / Q4 sections
  선택 파라미터. P3: Q5 validate 필터 / Q6 workspace_docs 개수 신호)
- 부수: S1 팩토리 중복(G14와 함께), S2 _hook_summary 소유 이동(Q1과 함께)
- 리뷰 소품 2건: A5-lite 죽은 코드(over_threshold property·to_dict — 삭제 또는
  실사용 배선), 배치 A-번호와 기존 A-번호(A1 전역 훅/A7 미저장 확인 등) 충돌
  문구 정리(session_io docstring 등).
- **착수 순서 확정(2026-09-07 사용자)** — 웨이브 6개, 순차:
  ① Q1(+S2)·Q2 + 리뷰 소품 2건 ② G1·G2 ③ G3 ④ G4·G5·G6 ⑤ G7·G8·G9·Q3·Q4
  ⑥ G11·G12·G14(+S1)·G15·G13·G10·G16·Q5·Q6.
- **배치 완료(2026-09-07)** — 웨이브 6개 전부 랜딩. G1~G16·Q1~Q6·S1·S2와
  리뷰 소품 2건이 소거됐다. 앞으로는 **새 GUI 기능을 넣을 때 대응 MCP 도구를
  같은 WP에서 함께 만든다**(패리티 원칙 — CLAUDE.md WP-MCP 섹션이 정본).
  다음 갭 목록이 다시 쌓이면 그때 배치로 묶는다.

## 7. 다음 배치 확정 항목 (2026-09-07 사용자 확정)

- **폴더형 템플릿 — files/ 동반**: 사용자 템플릿을 JSON 하나가 아니라
  **폴더**(`~/.daedalus/templates/<id>/` = `.daedalus.json` + `files/` +
  `skill-files/`)로도 인식하고, 템플릿에서 생성한 프로젝트를 **처음 저장할 때**
  동봉 폴더를 저장 위치로 복사한다(carry_files_dir 재사용 — Save As 동반과
  같은 관례, 목적지에 이미 있으면 불가침). 실증: ue-dev-template의
  `files/ue-build.ps1`이 지금은 딸려 가지 않아 손 복사가 필요하다.
  기존 단일 JSON 형태는 그대로 공존(파일=문서 규약 유지). 내장 3종은 파일이
  없으므로 무영향. 미저장 상태에서 템플릿 files를 어디 두는지(임시 보관 vs
  저장 시점까지 원본 참조)가 설계 결정 — 원본 참조 쪽이 단순하나 템플릿
  갱신/삭제와의 경쟁을 살펴야 한다.
